const versionCaja = document.querySelector('.version');
const versionTitle = document.querySelector('.h3');

/* mierdas que tiene que hacer esta porqueria */

/* 1. abrir y cerrar las versiones que se le de click */
versionTitle.addEventListener('click', e =>{

    versionCaja.classList.add('version_open');

})

/* 2. */